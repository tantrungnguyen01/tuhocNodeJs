<div class="mt-4">
    <h1>Trang Tin Tức</h1>
    <div class="row">
        {{#each tintuc}}
        <div class="col-sm-6 col-lg-6">
            <div class="card mt-4">
                <a href="/courses/{{this.slug}}">
                    <img class="card-img-top" src="{{this.thumbails}}" alt="Card image cap">
                </a>
                <div class="card-body">
                    <a href="/courses/{{this.slug}}">
                        <h5 class="card-title">{{this.name}}</h5>
                    </a>
                    <p class="card-text">{{this.description}}</p>
                    <a href="#" class="btn btn-primary">Go somewhere</a>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>